let numero = parseInt (prompt ("Digite um número para saber o resultado da multiplicação por ele mesmo."));
function verificarAlert(){
    let resultado = numero*numero
    alert (`O resultado é ${resultado}.`);
}